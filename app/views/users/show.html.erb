<% provide :title, @user.name %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= @user.name %>
        <span><%= @user.email %></span>
      </h1>
    </section>
  </aside>
  <%= link_to t("user.delete"), user_path, method: :delete, class: "btn btn-primary" %>
</div>
