<div class="row">
  <div class="col-md-10 col-offset-2">
    <h2><%= t "user.list_user" %></h2>
    <table class="table table-striped table-hover">
      <thead>
        <th><%= t "user.name" %></th>
        <th><%= t "user.email" %></th>
        <th><%= t "user.delete" %></th>
      </thead>
      <tbody>
        <% @users.each do |user| %>
          <tr>
            <td><%= user.name %></td>
            <td><%= user.email %></td>
            <td>
              <%= link_to [:admin, user], method: :delete, data: {confirm: t("user.sure")},
                class: "btn btn-primary btn-xs" do %>
                <span class="glyphicon glyphicon-eye-open"></span> <%= t "user.destroy_user" %>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
